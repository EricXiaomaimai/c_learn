#include <stdio.h>
int main(void)
{
    int num = 1;
    num = 1;

    printf("I am a simple ");
    printf("computer.\n");
    printf("My favorite number is %d because it is first.\n", num);

    // int value = num * 2;
    printf("Your favorite number is %d because it is second.\n", num * 2);

    return 0;
}